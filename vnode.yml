---
- name: Install Roles that are needed for the virtual cluster.
  hosts:
    - cluster
  become: True
  roles:
      #- spacewalk_client
    - cluster
      #- ldap

# - import_playbook: users.yml
