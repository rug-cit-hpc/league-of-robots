#
# The ~/.ssh/tmp/ directory is used to create sockets for SSH connection multiplexing as confgured in ~/.ssh/config.
# When there are no outgoing SSH connections in use, this folder may be empty, but do NOT remove it! 
#